spring:
  datasource:
    url: jdbc:h2:mem:poc_llm_ufc_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

  mail:
    host: localhost
    port: 25

  ai:
    openai:
      api-key: sk-test

security:
  jwt:
    secret: dGVzdC1zZWNyZXQta2V5LXBvYy1sbG0tdWZjLXRlc3RlLTIwMjQ=
    access-token-expiration: 900000
    refresh-token-expiration: 604800000
